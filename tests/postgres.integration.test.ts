// tests/postgres.integration.test.ts — removed (schema changed, needs rewrite)
import { describe, it } from 'vitest';
describe('postgres integration (removed)', () => { it.skip('not applicable', () => {}); });
